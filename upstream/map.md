<!-- mova:engine -->
# upstream/map.md — limba → mova, file by file

> The porting contract. Every file limba tracks appears here with its mova destination and
> the transform applied on the way. `playbooks/sync-upstream.md` resolves each new `PORT-NNN`
> entry in limba's `docs/logs/porting_log.md` against this table; a limba file missing from
> this table is a sync error, not a judgment call. Locked position: `upstream/limba.lock`.

**Transforms:**
- `verbatim` — port as-is (path may change).
- `param` — structure ports; named specifics move to the profile config block or the pack.
- `split` — one limba file becomes two+ mova files (engine half + pack/instance half).
- `template` — limba's file is personal content; its *shape* becomes a `setup/templates/`
  scaffold that setup regenerates per learner.
- `design-input` — not ported; informs a mova feature named in Notes.
- `skip` — personal history or limba-only plumbing; downstream never needs it.

## Root & infra

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `CLAUDE.md` | `AGENTS.md` | split | Generic operational guide (invariants 2–5, doc map, session hygiene, verb table) → AGENTS.md. Invariant 1 ("exam is the spec") → goal-contract templates. Invariant 6 + language policy: generic teaching/translation rules → `docs/mechanics/teaching.md`; UA/RU/EN contrast ranking → `setup/templates/profile.template.md`; diacritic look-alike rule → `packs/<code>/normalize.mjs` + a generic statement in `docs/mechanics/verification.md`. Commands block → AGENTS.md, regenerated per active scripts. |
| `AGENTS.md` | `CLAUDE.md` | verbatim | Pointer file, direction inverted: mova's canonical file is AGENTS.md. |
| `README.md` | `README.md` | template | Authored new for the template audience (first-5-minutes path). |
| `package.json` | `package.json` | param | name → mova; scripts/devDeps identical (vitest + typescript only). |
| `package-lock.json` | `package-lock.json` | verbatim | Regenerated by `npm install`. |
| `tsconfig.json` | `tsconfig.json` | verbatim | |
| `vitest.config.ts` | `vitest.config.ts` | param | Excludes agent worktrees from test collection. **Ported despite `Kind: infra`** — limba's reason is its own worktree layout, but the underlying rule is general: a suite's collection root and its data root must be the same tree, and every contract test here resolves data relative to itself. An instance inherits the defect the moment any agent opens a worktree under the repo root, and its human is a learner who has no correct action available when a sibling tree fails their close-out. `param` = the glob list, widened to `.worktrees/` as well as Claude Code's `.claude/worktrees/`. |
| `.gitignore` | `.gitignore` | param | Same categories: materials, tts-cache, recordings, .obsidian. |
| `.github/workflows/ci.yml` | `.github/workflows/ci.yml` | verbatim | Node 20, npm ci + npm test. |
| `.vscode/settings.json` | `.vscode/settings.json` | verbatim | Plain-text markdown rule — WYSIWYG editors flatten table-cell links; generic. |
| `.claude/launch.json` | `agents/claude-code/launch.json` | param | Visuals preview server; adapter-generated into instances configured for Claude Code. |

## Skills → playbooks

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `.claude/skills/limba-lesson/SKILL.md` | `playbooks/lesson.md` | param | Frontmatter → playbook frontmatter (`verb/summary/triggers/requires/scenarios`). RO trigger phrases → adapter generation data. |
| `.claude/skills/limba-drill/SKILL.md` | `playbooks/drill.md` | param | Size rule, sweep/loop modes, pending-system line: all generic. |
| `.claude/skills/limba-write/SKILL.md` | `playbooks/write.md` | param | "Article-only re-read" beat generalizes to "blind-spot re-read" driven by the instance's error taxonomy top codes. |
| `.claude/skills/limba-mock/SKILL.md` | `playbooks/mock.md` | param | "Real papers outrank generated" stays; instrument comes from `docs/reference/goal.md` §assessment. |
| `.claude/skills/limba-review/SKILL.md` | `playbooks/review.md` | param | Pace-vs-date branch gated on `goal_date`; volume/streak metrics when absent. Gains the ladder-recalibration checkpoint. |
| `.claude/skills/limba-vocab/SKILL.md` | `playbooks/vocab.md` | param | Row schema from pack (gender labels, required per-lexeme fact). |
| `.claude/skills/limba-retro/SKILL.md` | `playbooks/retro.md` | verbatim | Fully generic including the multi-writer concurrency protocol. Placement table's file names updated to mova paths. |
| — | `playbooks/setup.md` | new | The onboarding interview driver. |
| — | `playbooks/update.md` | new | Instance updater (hash manifest + CHANGELOG walk). |
| — | `playbooks/tutor-prep.md` | new | From `docs/projects/speaking_prep.md` (design-input). |
| — | `playbooks/sync-upstream.md` | new | Maintainer-only; consumes limba's porting log via this map. |

## docs/mechanics (the engine's core)

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `docs/mechanics/README.md` | `docs/mechanics/README.md` | verbatim | Provenance markers, "skills are verbs, mechanics are nouns", "reality only". |
| `docs/mechanics/session_format.md` | `docs/mechanics/session_format.md` + `docs/mechanics/why/session_format.md` | split | ~95% generic. RO examples kept but labelled as examples from the reference implementation. Diacritic normalization pointer → pack. Close-out step 11 (PORT entry) is limba-only — instances get the 12-step ritual without it. **Rules stay; provenance moves to `why/`** (PORT-013) — mova's own provenance splits on mova's own text, not limba's. |
| `docs/mechanics/srs.md` | `docs/mechanics/srs.md` + `docs/mechanics/why/srs.md` | split | Ladder + cap + cost model marked `default (measured on learner #1 — recalibrate)`. POS tag table → pack. "Verbs carry their eu-form" → pack-declared required-fact rule. Review-mode-by-tier structure stays. Rules/why split per PORT-014. |
| `docs/mechanics/teaching.md` | `docs/mechanics/teaching.md` + `docs/mechanics/why/teaching.md` | split | 8 beats, chat/visual split, translation rules: generic. "Mark what changes" gated on pack `inflection:` flag. dex.mjs fact-check step → dictionary-adapter step per `verification.md`. Rules/why split per PORT-013. |
| `docs/mechanics/media.md` | `docs/mechanics/media.md` + `docs/mechanics/why/media.md` | split | Voice ladder → pack manifest + `MOVA_VOICE`. afplay/say/whisper → capability flags. Artifact publishing → `publishing:` capability branch (local files are the base case). Rules/why split per PORT-014. The `U00` shelf line lands in the index-row rule (PORT-028); its reasoning and the offer's are `why/`. |
| `docs/mechanics/error_taxonomy.md` | `setup/templates/error_taxonomy.template.md` + `packs/ro/notes.md` | split | Code format, scope-section convention, tally tokens (generic skeleton) → template. The 24 RO codes → pack notes as reference material for pair-specific taxonomy generation. **PORT-020's generic half is the selection rule, not the code**: a near-neighbour earns its own code when its *repair route* differs, and sibling zones (two letter classes on one output path) are never fused. `ORTH-SEQ` itself is `ro-pack`. The provenance pressure ports as a budget row — `error_taxonomy.md` is in `docs/consequential.test.ts`'s BUDGET, skipping in template mode and binding the moment a learner has one, so the same why/ split is forced downstream. |
| — | `docs/mechanics/verification.md` | new | Fact-verification policy (dictionary-verified / tutor-confirmed / unverified `?` marker). |
| `docs/mechanics/why/*.md` | `docs/mechanics/why/*.md` | param | **The structure ports, the content does not.** A `why/<file>.md` holds the provenance of its own repo's rule file — limba's incidents are limba's. mova splits its own text the same way and keeps the same two contracts: the marker (*assumed* / *derived from* / *measured*) stays with the rule, and `why/` is uncapped. A `why/` file with no matching rule file is a CI failure, so provenance cannot outlive the rule it explains. |

## docs/reference + top-level docs → setup templates

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `docs/reference/learner.md` | `setup/templates/profile.template.md` | template | Shape: languages-held ranking, operational profile, "Measured, not assumed" accretion section. Plus the ONE machine-readable config block (new) absorbing the exam-date line, capability flags, `pack:`, `template_version:`. |
| `docs/reference/exam.md` | `setup/templates/goal-exam.template.md` | template | Deadline math shape, TO-CONFIRM list convention, single-owner date line (moves into profile config block). |
| — | `setup/templates/goal-level.template.md`, `goal-functional.template.md`, `goal-ledger.template.md` | new | The other goal-contract variants. |
| `docs/reference/transfer.md` | `setup/templates/transfer.template.md` | template | Mandated sections: script/phonology, morphology traps, syntax order, ≥10 false friends, register. All claims born `(assumed)`. |
| `docs/reference/topics.md` | `setup/templates/topics.template.md` | template | 5-column format, `T-NNNN` ids never reused, unit joins, "arbiter for repair-vs-new" role. |
| `docs/reference/resources.md` | `setup/templates/resources.template.md` | template | Registry-first convention; RO links stay in `packs/ro/notes.md`. |
| `docs/concept.md` | `setup/templates/concept.template.md` | template | Why/persona/non-goals shape; "the summary, not the record" pointer discipline. |
| `docs/plan.md` | `setup/templates/plan.template.md` | template | Phases, Projects Index mirror, Discovered bin, Standing rules mirror. |
| `docs/curriculum.md` | `setup/templates/curriculum.template.md` | template | Unit format, one `status:` line, rolling-wave rule (first 3 detailed). |
| `docs/projects/*.md` (9 files) | — | design-input | Personal specs. `speaking_prep.md` → `playbooks/tutor-prep.md`; `progress_visibility.md` → hub confidence panel; `workspace_infra.md` → this file's lineage section. Not ported as files. |

## Contract tests

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `docs/projects.index.test.ts` | `docs/projects.index.test.ts` | verbatim | CONTRACT block untouched. |
| `docs/plan.annotations.test.ts` | `docs/plan.annotations.test.ts` | verbatim | |
| `docs/logs.entries.test.ts` | `docs/logs.entries.test.ts` | param | CONTRACT: SES + ERR logs only (PORT log is limba-only). Gains the graded-check gate (PORT-024): a lesson entry with no parseable grammar score fails CI, with `<!-- no-graded-check: <reason> -->` as the on-the-record escape hatch. It imports `gradedScore` from `sources.mjs` rather than reimplementing it — **that import is the contract**, and it is what stops the two ends of the pipeline drifting the way they did upstream. Dated from `SPLIT_FROM`, one constant shared with the parser. |
| `docs/curriculum.test.ts` | `docs/curriculum.test.ts` | param | Heading regex built from pack `level_scale` + profile unit count. |
| `docs/topics.test.ts` | `docs/topics.test.ts` | param | `examRe` built from goal contract's section letters. |
| `docs/visuals.index.test.ts` | `docs/visuals.index.test.ts` | param | Gains `profile.html` in `CONTRACT.exclude` and the `Teaches` cap (PORT-025), imported from `page-shell.mjs` rather than restated — the cap is the card's clamp. `BUILT_MARKER` now imports `BUILT_RE` from `sources.mjs`: this file's own copy refused the bold form rows are written in, so a correctly staged page failed CI here while `unitState()` read it as staged. |
| `docs/visuals.content.test.ts` | `docs/visuals.content.test.ts` + `scripts/visualcheck.mjs` | split | Checks consolidated into visualcheck (usable pre-publish, not only in CI); test becomes a thin runner. Retired-claims table stays instance-owned in `work/visuals/README.md`. |
| `docs/consequential.test.ts` | `docs/consequential.test.ts` | param | SRS-ceiling arithmetic reads target from goal contract; "no future dates" and "no retired tool names" generic. |
| `state/ledgers.test.ts` | `state/ledgers.test.ts` | param | Look-alike map + POS requirement + required-lexeme-fact via `scripts/pack.mjs`. Column `ro` → `target`. |
| `scripts/inline-md.test.ts` | `scripts/inline-md.test.ts` | verbatim | |
| `work/feedback/insights.test.ts` | `work/feedback/insights.test.ts` | verbatim | |
| — | `docs/goal.test.ts` | new | Goal-contract structure (spec sentence, assessment instrument, deadline-or-rule). |
| `scripts/visual-shell.test.ts` | `scripts/visual-shell.test.ts` | param | Holds the two frame contracts: the shell defines every `CORE_TOKENS` name in all three theme blocks (derived from that array, so adding a gate token fails here until the frame defines it), and the shell ships no audio player. Scoped overrides and the shared focus ring are allowed — they need the player to exist, they do not define it. |
| — | `docs/agents.test.ts` | new | Adapter presence for configured agent; no rule text in adapters. |

## Scripts

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `scripts/queue.mjs` | `scripts/queue.mjs` | param | Column `ro` → `target`. INTERVALS/CAP stay tri-copied (srs.md, queue.mjs, ledgers test) per limba's move-together rule. |
| `scripts/tally.mjs` | `scripts/tally.mjs` | param | Code-name letter class widened per pack script. |
| `scripts/leakcheck.mjs` | `scripts/leakcheck.mjs` | verbatim | NFD+diacritic folding is universal. |
| `scripts/log-append.mjs` | `scripts/log-append.mjs` | param | LOGS map minus `porting`. |
| `scripts/feedback.sh` | `scripts/feedback.sh` | param | Env `LIMBA_FEEDBACK_FILE` → `MOVA_FEEDBACK_FILE`. |
| `scripts/inline-md.mjs` | `scripts/inline-md.mjs` | verbatim | |
| `scripts/pos.mjs` | `scripts/pos.mjs` + `packs/ro/pos-tables.mjs` | split | Generic driver (markPair diffing, classification loop, one-classifier-two-consumers doctrine) stays; TYPES, tag grammar, ENDINGS → pack. |
| `scripts/dex.mjs` | `packs/ro/dictionary.mjs` | split | Reimplemented against the adapter interface in `scripts/dictionary.mjs` (new, with null adapter). |
| `scripts/deck.mjs` | `scripts/deck.mjs` | param | Language strings/mode labels from profile+pack via `scripts/pack.mjs` (new). |
| `scripts/hub.mjs` | `scripts/hub.mjs` | param | Hardcoded RO/B1 strings from profile; exam-date regex → `scripts/profile.mjs` (new); `--ro/--ua` tokens → `--l1/--l2`. Gains the confidence panel. Its readers and frame move out to `sources.mjs` + `page-shell.mjs` (PORT-022); what stays is this page's editorial — the verdict, the queue meter, the unit board, Gains the `U00` cheat-sheet shelf (PORT-028) and the orphan report — a page filed under a unit the curriculum does not define is named on stdout, gated here on there being a curriculum at all, which limba never lacks. **The shelf slot diverges twice**: it renders through `when()` rather than limba's inline `shelf.length ? … : ""`, so the empty case is named instead of silent; and because a section that appears only after the learner has already made a cheat sheet can never tell them cheat sheets exist, the slot's first face is an offer, fired on live mistakes spanning three or more taught zones. Owed back to limba. **One deliberate divergence: the hub keeps its empty-state placeholders** rather than taking `when()` everywhere. limba's rule is that a fresh page must not inherit someone else's outline; mova's placeholders are not an outline but onboarding text naming the file that fills them, which a template's first instance genuinely needs. `when()` governs the profile page, where the sections are pure history. |
| `scripts/tally.mjs` (span) | `scripts/tally.mjs` | verbatim | Per-code first/last live sighting. Live-only: a banked occurrence has been answered by a clean re-test, and dating a zone by it reports the problem as older than it is. |
| `scripts/speak.sh` | `scripts/speak.sh` | param | Voice from pack manifest; `MOVA_VOICE` override. |
| `scripts/tts-embed.mjs` | `scripts/tts-embed.mjs` | param | Same voice sourcing; sha1-keyed cache unchanged. |
| `scripts/tts-warm.mjs` | `scripts/tts-warm.mjs` | param | Same. |
| `scripts/pronounce.sh` | `scripts/pronounce.sh` | param | Whisper language flag from pack; capability-gated (macOS mic pipeline). |
| — | `scripts/profile.mjs` | new | Parses the profile config block; the single reader every script and CONTRACT block uses. |
| — | `scripts/pack.mjs` | new | Loads the active pack's modules per profile `pack:`. |
| — | `scripts/dictionary.mjs` | new | Adapter interface + null adapter. |
| — | `scripts/factcheck.mjs` | new | Batch-verifies ledger facts via the adapter; feeds the hub. |
| — | `scripts/packcheck.mjs` | new | Structural pack validator + golden fixtures runner. |
| — | `scripts/visualcheck.mjs` | new | Consolidated visual validator (see tests above). |
| — | `scripts/manifest.mjs` | new | Writes `upstream/manifest.json` (engine-file hashes for instance `/update`). |
| `scripts/sources.mjs` | `scripts/sources.mjs` | param | The readers every generated page shares, extracted from `hub.mjs` (PORT-022). **Profile-free and pack-free here, unlike limba's** — `commands(focus)` takes the personal parameter rather than reading it, which is what lets `docs/logs.entries.test.ts` import `gradedScore` and hold the close-out template to the parser. `units()` also parses the can-do line, which the hub's in-progress band states. |
| `scripts/page-shell.mjs` | `scripts/page-shell.mjs` | param | The dashboard frame. Gains the `reference` kind pill (PORT-028) — limba tints it with a morpheme token mova has no equivalent of, so it takes a dotted edge instead — and the `shelf` anchor. limba's imports `TOKENS` from its `visual-shell.mjs`; mova's `visual-shell.mjs` holds no palette of its own — it reads `docs/visual/starter.html` — so a `tokens()` export was added there and this module imports that. Same rule, one more level of indirection, because mova's single definition is the pinned theme file. Gains `ANCHOR`/`anchorOf`/`linkTo` (PORT-027) and `TEACHES_MAX` (PORT-025), which `docs/visuals.index.test.ts` imports so the cap cannot part company from the clamp. |
| `scripts/profile.mjs` (the page) | `scripts/profilepage.mjs` | param | **Renamed — the one name collision in the map.** mova already owns `scripts/profile.mjs`: the reader of the learner config block, which every script imports and limba has no equivalent of. The page generator takes the longer name. Section list gated by `when()`, so a downstream instance shows only what it has. |
| `scripts/sources.test.ts` | `scripts/sources.test.ts` | param | Split: the pure functions (`must`/`when`/`unitState`/`BUILT_RE`/`weekStart`/`gradedScore`) are fixture-checked and hold in the template repo; the readers that touch instance files skip in template mode, where `docs/curriculum.md` and the ledger rows do not exist. |
| `scripts/page-shell.test.ts` | `scripts/page-shell.test.ts` | param | Same two frame contracts as limba's, plus: `nav()` names every generated page from each of them, marks exactly one, and carries `tohub` on the hub entry — mova's `visualcheck` check 5 requires that link on every page, so the nav satisfies it by construction instead of a second element going stale. |
| `scripts/closeout.mjs` | `scripts/closeout.mjs` | param | The mechanical half of the close-out as three commands (`--start` at orient, `--brief`, `--finish`). Templates it prints come from the instance's own conventions; the porting-log template is limba-only and is dropped. **Two invariants survive the port intact:** it never commits (prints a path-named `git add`) and never writes a log (`log-append.mjs` owns IDs inside its lock). |
| `scripts/visual-shell.mjs` | `scripts/visual-shell.mjs` | param | One definition of a teaching page's frame. In mova the token set is derived from `visualcheck.mjs`'s `CORE_TOKENS` and the theme is the pinned `docs/visual/tokens.css`; `--l1/--l2` labels come from the profile. Inlined at build time, never linked — a page must render offline forever. |
| `scripts/newvisual.mjs` | `scripts/newvisual.mjs` | param | `node scripts/newvisual.mjs <slug> [--vocab]` writes a skeleton that passes every `visualcheck` gate before any content exists. Supersedes copy-from-`starter.html` as the way a page begins; `docs/visual/starter.html` stays as the readable reference the generator is checked against. |

## State, work, logs

| limba | mova | transform | notes |
| --- | --- | --- | --- |
| `state/vocab.md` | `state/vocab.md` | template | Header row only; column `ro` → `target`. |
| `state/grammar.md` | `state/grammar.md` | template | Same. |
| `docs/logs/README.md` | `docs/logs/README.md` | param | Porting-log row is limba-only. |
| `docs/logs/session_log.md` | `docs/logs/session_log.md` | template | Intro blockquote only; entries are instance history. |
| `docs/logs/error_log.md` | `docs/logs/error_log.md` | template | Same. |
| `docs/logs/porting_log.md` | — | skip | limba-only: the upstream feed this repo consumes. |
| `docs/snapshots/README.md` | `docs/snapshots/README.md` | verbatim | "Not exercised:" convention is core engine. |
| `docs/snapshots/2026-07-30_placement.md` | — | skip | Personal history. |
| `work/visuals/README.md` | `docs/visual/SPEC.md` + `setup/templates/visuals-readme.template.md` | split | Conventions (tokens, audio-in-row, two-layer reveal, morpheme marking, hub link) → SPEC. Index table + retired-claims table → instance template, which gains the `## Build notes` section and the title-is-not-the-slug rule (PORT-025). **SPEC.md also takes the generated-page rules** — registration in four places, `must()`/`when()`, one frame definition, declared anchors. limba puts those in `media.md`; mova's word budget refused them, and the budget was right: a session that builds a *teaching* page never needs them, so they belong with the page spec, not in the per-session read. The `Kind` vocabulary and the `U00` shelf rule (PORT-028) go to the template, where the index schema is stated. |
| `work/visuals/*.html` (15 authored pages) | — | design-input | Personal content. Component patterns (reveal pairs, tts rows, morpheme marking, deck rows) extracted into `docs/visual/starter.html` + `gallery.html` (2026-08-12_acord.html is the reference implementation). **Style layer comes from Hallmark** (nutlope/hallmark, MIT — usehallmark.com): setup uses its custom-theme guidance to craft the instance theme ONCE, pinned into `docs/visual/tokens.css`; pages then reuse the pinned theme so the workspace reads as one system, and stay self-contained/offline (Hallmark is a build-time skill, never a page dependency). limba's functional tokens (--l1/--l2/--new, gender, --mk/--st) survive as the semantic layer on top. |
| `work/visuals/deck.html`, `index.html` | — | skip | Generated files; generators are ported instead. |
| `work/feedback/PROMPT.md` | `work/feedback/PROMPT.md` | param | Retro intake conventions. |
| `work/feedback/insights.md` | `work/feedback/insights.md` | template | Header only. |
| `work/feedback/2026-08-07_retro.md`, `2026-08-09_leak-audit.md`, `2026-08-12_housekeeping.md` | — | skip | Personal history (the leak-audit's *rules* already live in session_format.md). |
| `work/sets/*` (4 files) | — | skip | Personal history; set format is specified in session_format.md + leakcheck. |
| `work/listening/.gitkeep`, `work/speaking/.gitkeep`, `work/writing/.gitkeep` | same paths + `work/tutor/.gitkeep` | verbatim | |
| `materials/README.md` | `materials/README.md` | param | Copyright policy + acquisition checklist shape; RO sources → `packs/ro/notes.md`. |

## Lineage

mova forks limba's machinery the way limba forked vibecoding-starterpack — but with this map
kept total and a live porting feed, because limba is still moving. Three deliberate
inversions relative to limba: AGENTS.md is canonical (multi-agent audience), the learner
profile is one file with one machine-readable block (limba: four prose copies + one regex),
and skills' bodies live agent-neutrally in `playbooks/` (limba: Claude Code skills).
